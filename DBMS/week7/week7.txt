Palindrome:

set serveroutput on
declare
    strinput varchar2(10) := '&strinput';
    palindrome varchar2(10);
BEGIN
    for i in REVERSE 1..LENGTH(strinput) loop
        palindrome:=palindrome||substr(strinput,i,1);
    end loop;
    if(strinput=palindrome) then
        dbms_output.put_line('The string is palindrome '||palindrome);
    ELSE
        dbms_output.put_line('The string is not palindrome '||palindrome);
    end if;
end;
/



Enter value for strinput: malayalam
old   2:     strinput varchar2(10) := '&strinput';
new   2:     strinput varchar2(10) := 'malayalam';
The string is palindrome malayalam

Enter value for strinput: kamesh
old   2:     strinput varchar2(10) := '&strinput';
new   2:     strinput varchar2(10) := 'kamesh';
The string is not palindrome hsemak.


Adding :

SQL> SET serveroutput ON;
SQL> DECLARE
  2      nos NUMBER := &no;
  3      sum_of_digits NUMBER(8) := 0;
  4  BEGIN
  5      WHILE nos > 0 LOOP
  6          sum_of_digits := sum_of_digits + MOD(nos, 10);
  7          nos := nos / 10;
  8      END LOOP;
  9      DBMS_OUTPUT.put_line('Number =' || sum_of_digits);
 10  END;
 11  /

Enter value for no: 23
old   2:     nos NUMBER := &no;
new   2:     nos NUMBER := 23;
Number =5


Employee:

set severoutput on;
DECLARE
    v_emp emp%rowtype;
    v_dept dept%rowtype;
    v_empno emp.empno%type;
BEGIN
    v_empno := &v_empno;
    SELECT * INTO v_emp FROM emp WHERE empno = v_empno;
     SELECT dname INTO v_dept.dname FROM dept WHERE dno = v_emp.deptno;
    dbms_output.put_line(v_emp.ename||' works in '||v_dept.dname||' department and draws '||v_emp.sal||'/- as salary.');
END;
/


Enter value for v_empno: 123
old   6:     v_empno := &v_empno;
new   6:     v_empno := 123;
MAHESH works in IT department and draws 35000/- as salary.



employee 2:

SQL> set serveroutput on;
SQL> declare
  2      v_emp emp%rowtype;
  3      v_empno emp.empno%type;
  4      v_empsal empsal%rowtype;
  5  BEGIN
  6      select * into v_emp from emp where empno=&v_empno;
  7      v_empsal.hra:=v_emp.sal*0.5;
  8      v_empsal.da:=v_emp.sal*0.2;
  9      v_empsal.gross:=v_empsal.hra+v_empsal.da;
 10      v_empsal.pf:=v_emp.sal*0.12;
 11      v_empsal.net:=v_empsal.gross-v_empsal.pf;
 12      INSERT into empsal VALUES(v_emp.empno,v_emp.ename,v_emp.sal,v_empsal.hra,v_empsal.da,v_empsal.pf,v_empsal.gross,v_empsal.net);
 13      Dbms_output.put_line('Record inserted successfully');
 14  END;
 15  /


Enter value for v_empno: 106
old   6:     select * into v_emp from emp where empno=&v_empno;
new   6:     select * into v_emp from emp where empno=106;
Record inserted successfully

PL/SQL procedure successfully completed.


Area of Circle :

SQL> SET SERVEROUTPUT ON
SQL> DECLARE
  2      radius NUMBER(5,2) := &radius;
  3      area NUMBER(5,2);
  4      pi NUMBER(3,2) := 3.14;
  5  BEGIN
  6      area := radius * radius * pi;
  7      DBMS_OUTPUT.PUT_LINE('Area is: ' || area);
  8
  9      -- Insert the values into the table
 10      INSERT INTO circle (radius, area) VALUES (radius, area);
 11
 12      COMMIT; -- Commit the transaction
 13  END;
 14  /
Enter value for radius: 5
old   2:     radius NUMBER(5,2) := &radius;
new   2:     radius NUMBER(5,2) := 5;
Area is: 78.5

PL/SQL procedure successfully completed.